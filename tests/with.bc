support ::= module("support/module.bc");

temp := "hello world";

item ::= struct {
    name: string;
};

player ::= struct {
    name: string;
    items: []item;
};

p: player;

with p {
    for i in .items {
        with .i {
            support::print("player '%s' has item '%s'\n", p.name, .name):
        };
    };
};