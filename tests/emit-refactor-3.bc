core :: module("../modules/core");

fib :: proc(n: u64 := 10): u64 {
    core::print("n := %d\n", n);
    if n < 2 {
        return n;
    } else {
        n1 := n - 1;
        n2 := fib(n - 2);
        return fib(n1 + n2);
    };
};

//#run fib(4);