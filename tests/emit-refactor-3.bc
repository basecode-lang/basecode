core :: module("core");

player :: struct {
    pos: vector3;
    dir: vector3;
    lives: u8;
    level: u8;
    hp: u16;
    xp: u16;
};

vector3 :: struct {
    x: f32 := 1.0;
    y: f32 := 1.0;
    z: f32 := 1.0;
};

flags :: tuple(a := true, b := true, name := "test");

numbers: [5]u16 := array<u16>(1, 2, 3, 4, 5);
temp := numbers;

foo: u32;

p: player;

core::print("foo := %d\n", foo);
core::print("p.pos.x := %f\n", p.pos.x);
core::print("p.pos.y := %f\n", p.pos.y);
core::print("p.pos.z := %f\n", p.pos.z);
core::print("flags.name := %s\n", flags.name);