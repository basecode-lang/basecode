core :: module("core");

foo: u32;
bar: u8 := 2;

// XXX: there's a parser/ast_evaluator bug here
//      the fix here is to wrap the RHS in an expression group
//foo *:= (cast<u32>(bar) + 4);

// this works fine
foo := foo * cast<u32>(bar) + 4;

core::print("foo := %d\n", foo);
