support :: module("support/module.bc");

s2 := "oh, noes!  what did you DO!?";

fib :: proc(n: u64 := 10): u64 {
    if n == 0 || n == 1 {
        return n;
    } else {
        value := fib((n - 1) + fib(n - 2));
        support::print("value := %d\n", value);
        return value;
    };
};

//result := fib();
result := 0;
support::assert(result == 1 || result == 0,  address_of(s2));