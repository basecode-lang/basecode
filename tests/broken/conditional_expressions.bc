@library "libalpha-core.dylib";
#foreign print := proc(format: string, values: ...any) @alias "fmt_print";

// XXX: if blocks are not generating in an order conducive to sequential emit
//      if_element should be responsible for emitting the true/false blocks; not program
//if a == 32 {
//    print("we have a match!\n");
//} else {
//    print("no match\n");
//};
